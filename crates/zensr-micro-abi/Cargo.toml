[package]
name = "zensr-micro-abi"
version = "0.0.1"
edition = "2021"
license = "AGPL-3.0-only OR LicenseRef-Imazen-Commercial"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
zensr-micro = { path = "../zensr-micro", features = ["internals"] }
